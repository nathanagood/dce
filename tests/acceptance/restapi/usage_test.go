package apitests

import (
	"testing"
)

func TestUsageAPI(t *testing.T) {

	t.Run("GIVEN the usage endpoint", func(t *testing.T) {

		t.Run("WHEN an HTTP GET request is sent to /usage with the startDate query parameter", func(t *testing.T) {

			t.Run("THEN expect a 200 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 400 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 403 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 500 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

		})

		t.Run("WHEN an HTTP GET request is sent to /usage with the endDate query parameter", func(t *testing.T) {

			t.Run("THEN expect a 200 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 400 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 403 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 500 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

		})

		t.Run("WHEN an HTTP GET request is sent to /usage with the principalId query parameter", func(t *testing.T) {

			t.Run("THEN expect a 200 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 400 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 403 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 500 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

		})

		t.Run("WHEN an HTTP GET request is sent to /usage with the limit query parameter", func(t *testing.T) {

			t.Run("THEN expect a 200 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 400 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 403 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 500 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

		})

		t.Run("WHEN an HTTP GET request is sent to /usage with the nextPrincipalId query parameter", func(t *testing.T) {

			t.Run("THEN expect a 200 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 400 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 403 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 500 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

		})

		t.Run("WHEN an HTTP GET request is sent to /usage with the nextStartDate query parameter", func(t *testing.T) {

			t.Run("THEN expect a 403 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 500 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 200 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

			t.Run("THEN expect a 400 response", func(t *testing.T) {
				// TODO: Put in code here to customize the functional test

			})

		})

	})

}
